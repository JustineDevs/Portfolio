# Release v0.1.5

Release notes for `v0.1.5` (since `v0.1.4`).

## Highlights

- Added the public `/changelog` page using the repository's release notes as its source of truth.
- Expanded the CMS-backed portfolio foundation with reusable asset, content, experience, home, legal, and provider-usage surfaces.
- Fixed confirmed admin hydration and sidebar interaction defects while preserving the established JSTN public navbar design.

## Changed

- Added the reusable media asset registry and provider connection/usage foundations.
- Added CMS routes and editor surfaces for home, experience, content, connections, and the asset library.
- Added the changelog to Footer → Resources only, keeping the primary navbar unchanged.

## Verification

- `pnpm exec tsc --noEmit`
- `pnpm lint`
- `pnpm test`
- `pnpm build`
- `git diff --check`

